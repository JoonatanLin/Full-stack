```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a new note into the input field
    Note right of browser: User clicks "Save"

    Note right of browser: SPA JavaScript intercepts the form submit\n(prevents default page reload)
    Note right of browser: JS creates a new note object (content + date)\nand updates the UI optimistically

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa\n(JSON payload: {content: "...", date: "..."})
    activate server
    Note right of server: Server saves the new note
    server-->>browser: 201 Created (or 200 OK)\n(response confirms save)
    deactivate server

    Note right of browser: Browser stays on the same page (no redirect)\nUI already shows the new note (or updates after response)
